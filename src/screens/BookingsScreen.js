import React from "react";
import { View, Text, StyleSheet } from "react-native";

const BookingsScreen = () => {
  return <Text>Bookings Screen</Text>;
};

const styles = StyleSheet.create({});

export default BookingsScreen;
